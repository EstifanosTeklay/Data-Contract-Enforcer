kind: DataContract
apiVersion: v3.0.0
id: week4-lineage-snapshots
info:
  title: Week 4 Brownfield Cartographer — Lineage Snapshots
  version: 1.0.0
  owner: week4-team
  description: Auto-generated contract for week4 outputs. Generated at 2026-04-04T20:45:26.060587Z
    from 1 records.
servers:
  local:
    type: local
    path: outputs/week4/lineage_snapshots.jsonl
    format: jsonl
terms:
  usage: Internal inter-system data contract. Do not publish.
  limitations: confidence fields must remain float 0.0-1.0. Breaking changes require
    blast radius report.
schema:
  snapshot_id:
    type: string
    required: true
    description: 'Column snapshot_id — null fraction: 0.0%'
    format: uuid
    unique: true
  codebase_root:
    type: string
    required: true
    description: 'Column codebase_root — null fraction: 0.0%'
  git_commit:
    type: string
    required: true
    description: Exactly 40 hex characters.
    pattern: ^[a-f0-9]{40}$
  nodes:
    type: string
    required: true
    description: 'Column nodes — null fraction: 0.0%'
  edges:
    type: string
    required: true
    description: 'Column edges — null fraction: 0.0%'
  captured_at:
    type: string
    required: true
    description: 'Column captured_at — null fraction: 0.0%'
    format: date-time
quality:
  type: SodaChecks
  specification:
    checks for week_4:
    - missing_count(snapshot_id) = 0
    - duplicate_count(snapshot_id) = 0
    - row_count >= 1
lineage:
  upstream: []
  downstream:
  - id: week7-violation-attributor
    description: ViolationAttributor traverses lineage graph for blame chain
    fields_consumed:
    - nodes
    - edges
    - git_commit
    breaking_if_changed:
    - edges.source
    - edges.target
    - nodes.node_id
